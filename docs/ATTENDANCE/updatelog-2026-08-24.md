# Updatelog 2026-08-24

## 变更

- 系统设置新增“有效时间”选项，用于配置双认证周期内有效考勤时间取值。
- 默认保持旧逻辑：取工牌与人脸两条认证记录中较晚的一条。
- 新增“最早一次认证”模式：取工牌与人脸两条认证记录中较早的一条。
- 有效考勤溯源快照新增 `effective_time_rule_mode`，便于排查当时使用的是 `latest` 还是 `earliest`。

## 注意

- 切换设置后立即影响后续新产生的考勤计算。
- 本次补丁不自动重算已生成的有效考勤和日报；管理员手动重算历史日期时，会按重算时的当前设置执行。
